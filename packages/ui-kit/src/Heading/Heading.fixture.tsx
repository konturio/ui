import { Heading } from '.';

export default (
  <div style={{ display: 'flex', flexFlow: 'column nowrap', gap: '2em', maxWidth: '400px' }}>
    <Heading type="heading-01">This is for component and layout headings</Heading>
    <Heading type="heading-02">This is for component and layout headings</Heading>
    <Heading type="heading-03">This is for component and layout headings</Heading>
    <Heading type="heading-04">This is for component and layout headings</Heading>
    <Heading type="heading-05">This is for component and layout headings</Heading>
    <Heading type="heading-06">This is for component and layout headings</Heading>
  </div>
);
