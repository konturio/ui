import { MCDALegend } from '.';

export default (
  <div style={{ width: '210px', border: '2px solid hsla(0, 0%, 0%, 0.2)', padding: '8px', borderRadius: '8px' }}>
    <MCDALegend
      title="Legend"
      subtitle="Hexagons are colored as weighted average of normalized and transformed layers values"
    />
  </div>
);
